void main() {
  List<int> daftarNilai = [1, 2, 3, 4, 5];

  // Menggunakan perulangan for-in untuk mencetak elemen dalam urutan terbalik
  for (var nilai in daftarNilai.reversed) {
    print(nilai);
  }
}
